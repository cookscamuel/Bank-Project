
validation for registering
SELECT userId, email, password, name, address, phone FROM users WHERE email="$email" AND password="$password";

all accounts related to a user
SELECT * FROM active_accounts WHERE user_id="$userId";

retrieve balance
SELECT balance FROM accounts WHERE account_number="$accountNumber";

update balance both ways
UPDATE active_accounts SET balance="$balance" WHERE account_number="$accountNumber";