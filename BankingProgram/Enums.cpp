// To Use when the program starts up
enum LoginChoice{
    LOGIN = 1,
    REGISTER
};

// to use when an account is being created
enum AccountChoice{
    CHECKING = 1,
    SAVINGS,
    FIXED_DEPOSIT
};
